[project]
name = "tofsims-formula-network"
version = "0.1.0"
requires-python = ">=3.10"
dependencies = ["rdkit", "pandas", "pyyaml", "networkx", "tqdm"]

[tool.pytest.ini_options]
testpaths = ["tests"]
